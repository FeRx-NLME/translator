% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/translate.R
\name{to_ferx}
\alias{to_ferx}
\alias{nm_to_ferx}
\alias{nlmixr2_to_ferx}
\alias{mlx_to_ferx}
\title{Translate a pharmacometric model to ferx format}
\usage{
to_ferx(
  source,
  format = c("nonmem", "nlmixr2", "monolix"),
  output = NULL,
  overwrite = FALSE
)

nm_to_ferx(ctl_file, output = NULL, ...)

nlmixr2_to_ferx(model_fn, output = NULL, ...)

mlx_to_ferx(mlxtran, output = NULL, ...)
}
\arguments{
\item{source}{For \code{"nonmem"}: path to a \code{.ctl} file. For \code{"nlmixr2"}: an
nlmixr2/rxode2 model function. For \code{"monolix"}: path to a \code{.mlxtran} file.}

\item{format}{Source format. One of \code{"nonmem"}, \code{"nlmixr2"}, \code{"monolix"}.}

\item{output}{Optional path for the output \code{.ferx} file. If \code{NULL} (default)
the result is returned but not written.}

\item{overwrite}{If \code{output} is set and the file already exists, pass \code{TRUE}
to overwrite it. Default \code{FALSE}.}

\item{ctl_file}{Path to a NONMEM control stream file.}

\item{model_fn}{An nlmixr2 / rxode2 model function.}

\item{mlxtran}{Path to a Monolix \code{.mlxtran} project file.}
}
\value{
A \code{ferx_translate_result} invisibly, with fields \verb{$ferx_text},
\verb{$warnings}, and \verb{$unsupported}.
}
\description{
Parses \code{source} via the appropriate intermediary (\code{nonmem2rx}, \code{rxode2}, or
\code{monolix2rx}), converts to a \link{ferx_ir}, emits a \code{.ferx} string, and
optionally writes it to disk.
}
\examples{
\dontrun{
# From NONMEM
result <- nm_to_ferx("run001.ctl", output = "run001.ferx")
result$warnings

# From nlmixr2
f <- function() {
  ini({ tvcl <- 0.134; eta.cl ~ 0.07; err.prop ~ 0.01 })
  model({ cl <- tvcl * exp(eta.cl); linCmt() ~ prop(err.prop) })
}
result <- nlmixr2_to_ferx(f)
cat(result$ferx_text)

# From Monolix
result <- mlx_to_ferx("project.mlxtran")
}
}
\seealso{
\code{\link[=write_ferx]{write_ferx()}}, \code{\link[=rxui_to_ir]{rxui_to_ir()}}, \code{\link[=emit_ferx]{emit_ferx()}}
}
