% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ir.R
\name{new_ferx_ir}
\alias{new_ferx_ir}
\title{Create a ferx intermediate representation}
\usage{
new_ferx_ir(
  source_format = NA_character_,
  source_file = NA_character_,
  thetas = list(),
  omegas = list(),
  kappas = list(),
  sigmas = list(),
  indiv_params = list(),
  structural = list(),
  odes = list(),
  initial_conditions = list(),
  diffusion = list(),
  error_model = list(),
  error_suggestion = character(),
  scaling = list(),
  fit_options = list(),
  warnings = character(),
  unsupported = character(),
  state_renames = character()
)
}
\arguments{
\item{source_format}{One of \code{"nonmem"}, \code{"nlmixr2"}, \code{"monolix"}, \code{"mrgsolve"},
or \code{NA}.}

\item{source_file}{Path to the source file, or \code{NA}.}

\item{thetas}{List of theta entries. Each element is a list with fields
\code{name} (character), \code{init} (numeric), \code{lower} (numeric), \code{upper} (numeric).}

\item{omegas}{List of omega entries. Each element is a list with \code{type}
(\code{"diagonal"} or \code{"block"}), \code{names} (character vector), and \code{values}
(numeric). For \code{"diagonal"}: one name, one value. For \code{"block"}:
multiple names, lower-triangular values.}

\item{kappas}{List of IOV kappa entries. Each element is a list with
\code{name} (character) and \code{value} (numeric).}

\item{sigmas}{List of sigma entries. Each element is a list with \code{name}
(character), \code{value} (numeric), and \code{scale} (\code{"sd"} or \code{"var"}).}

\item{indiv_params}{Ordered list of statements. Each element is a list with
a \code{kind}: \code{"assign"} (\code{lhs}, \code{rhs}, both character) or \code{"if"} (\code{cond},
character; \code{then}, and optionally \code{else_}, each a nested statement list).
An element with no \code{kind} is read as an \code{"assign"}, which is the shape this
field held before statement lists. Order is preserved exactly.}

\item{structural}{List describing the structural model. Must have \code{type}:
\code{"pk_macro"} (add \code{pk_call} and \code{pk_args}) or \code{"ode"} (add \code{obs_cmt}
and \code{states}). May be empty during incremental construction.}

\item{odes}{Ordered list of statements forming the \verb{[odes]} block. Each
element is a list with a \code{kind}: \code{"ddt"} (\code{state}, \code{rhs}), \code{"assign"}
(\code{lhs}, \code{rhs}) for an ODE-block intermediate, or \code{"if"} (\code{cond}, \code{then},
and optionally \code{else_}). An element with no \code{kind} is read as a \code{"ddt"},
which is the shape this field held before statement lists. Used only when
\code{structural$type == "ode"}.

Order is preserved exactly and nothing reorders it. ferx has no
use-before-def check in \verb{[odes]}: an intermediate placed below the \code{d/dt}
line that reads it stays valid, reads a stale slot, and collapses the
prediction to a constant with no diagnostic.}

\item{initial_conditions}{List of ODE initial-condition entries. Each element
is a list with \code{state} (character, an emitted state name) and \code{rhs}
(character, the expression). Rendered as \verb{init(<state>) = <rhs>} inside the
\verb{[odes]} block. ferx resolves an init expression against individual
parameters, other states and literals only -- not thetas -- so
\code{\link[=rxui_to_ir]{rxui_to_ir()}} drops and reports one that would reference anything else
rather than emitting a file the engine rejects.}

\item{diffusion}{List of diffusion entries. Each element is a list with
\code{state} (character) and \code{value} (numeric).}

\item{error_model}{List of error model entries. Each element is a list
with \code{dv} (character), \code{type} (\code{"proportional"}, \code{"additive"}, or
\code{"combined"}), and \code{params} (character vector of parameter names). An entry
may additionally carry \code{cmt} (integer, rendered as \code{CMT=N: DV ~ ...}) or
\code{cond} (character, rendered as an \code{if}/\verb{else if} chain whose last entry
omits \code{cond} and becomes the \verb{else} ferx requires). The three forms are
mutually exclusive within one list.}

\item{error_suggestion}{Character vector of comment lines rendered where the
\verb{[error_model]} block would have gone, when the source error expression
could not be translated. The block itself is omitted so the engine rejects
the file rather than accepting a guess; these lines carry the source
expression and a plausible reading the user can uncomment after checking it.}

\item{scaling}{List describing the observation readout. At most one of
\code{obs_scale} (numeric or character, the NONMEM \code{S1}/\code{S2} scale), \code{y} (a
Form C readout expression as a single string) or \code{per_cmt} (a list of
\code{list(cmt =, expr =)} entries rendered as \code{y[CMT=N] = ...}). \code{obs_scale}
cannot be combined with either readout: ferx applies it on top of them.}

\item{fit_options}{List with named elements such as \code{method}, \code{maxiter},
\code{covariance}, and (when IOV is present) \code{iov_column}.}

\item{warnings}{Character vector of diagnostic messages, each prefixed
with \code{INFO}, \code{WARN}, or \code{ERROR}.}

\item{unsupported}{Character vector of features detected in the source
that could not be translated.}

\item{state_renames}{Named character vector of ODE state renames, source
name -> emitted name. Provenance, not a diagnostic: the emitted \code{.ferx} is
the artefact that gets shared, and a reader holding only that file cannot
otherwise map a sanitised state back to the source compartment it came
from. Rendered as \verb{# renamed:} comments by \code{\link[=emit_ferx]{emit_ferx()}}.}
}
\value{
A \code{ferx_ir} list.
}
\description{
Constructs a \code{ferx_ir} object that captures every concept expressible in a
\code{.ferx} file. All parsers produce this; the emitter consumes it. Names are
stored ferx-native (uppercase, underscored) from the moment of ingestion.
}
\examples{
ir <- new_ferx_ir(
  source_format = "nonmem",
  thetas = list(list(name = "TVCL", init = 0.134, lower = 0.001, upper = 10)),
  omegas = list(list(type = "diagonal", names = "ETA_CL", values = 0.07)),
  structural = list(type = "pk_macro", pk_call = "one_cpt_oral",
                    pk_args = list(cl = "CL", v = "V", ka = "KA")),
  error_model = list(list(dv = "DV", type = "proportional", params = "PROP_ERR")),
  fit_options = list(method = "foce", maxiter = 300L, covariance = TRUE)
)
print(ir)
}
\seealso{
\code{\link[=validate_ferx_ir]{validate_ferx_ir()}}, \code{\link[=emit_ferx]{emit_ferx()}}, \code{\link[=rxui_to_ir]{rxui_to_ir()}}
}
