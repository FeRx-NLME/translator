% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emit_ferx.R
\name{emit_ferx}
\alias{emit_ferx}
\title{Emit a .ferx file from a ferx intermediate representation}
\usage{
emit_ferx(ir)
}
\arguments{
\item{ir}{A \code{ferx_ir} object.}
}
\value{
A single character string (the complete \code{.ferx} file content).
}
\description{
Takes a validated \code{ferx_ir} and returns a single character string ready to
write as a \code{.ferx} file. Does not perform file I/O; use \code{\link[=write_ferx]{write_ferx()}} for that.
}
\examples{
ir <- new_ferx_ir(
  source_format = "nonmem",
  thetas      = list(list(name = "TVCL", init = 0.134, lower = 0.001, upper = 10)),
  omegas      = list(list(type = "diagonal", names = "ETA_CL", values = 0.07)),
  sigmas      = list(list(name = "PROP_ERR", value = 0.01, scale = "sd")),
  indiv_params = list(list(lhs = "CL", rhs = "TVCL * exp(ETA_CL)")),
  structural  = list(type = "pk_macro", pk_call = "one_cpt_oral",
                     pk_args = list(cl = "CL", v = "V", ka = "KA")),
  error_model = list(list(dv = "DV", type = "proportional", params = "PROP_ERR")),
  fit_options = list(method = "foce", maxiter = 300L, covariance = TRUE)
)
cat(emit_ferx(ir))
}
\seealso{
\code{\link[=new_ferx_ir]{new_ferx_ir()}}, \code{\link[=validate_ferx_ir]{validate_ferx_ir()}}, \code{\link[=write_ferx]{write_ferx()}}
}
